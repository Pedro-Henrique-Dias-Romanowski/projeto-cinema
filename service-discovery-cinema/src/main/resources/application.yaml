server:
  port: 8761
management:
  endpoint:
    health:
      probes:
        enabled: true
  health:
    livenessstate:
      enabled: true
    readinessstate:
      enabled: true
spring:
  application:
    name: service-discovery-cinema
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
